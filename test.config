# Add ENDPOINT and TOKEN to ~/.binary_test.conf
#   ENDPOINT=[your_endpoint]
#   TOKEN=[your_token]

# This line reads the above variables, we do that to avoid accidentally pushing tokens to github
. ~/.binary_test.conf
LIB_PATH="`pwd`/lib"

export ENDPOINT TOKEN LIB_PATH
